# 小说写作状态注入系统实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 实现状态注入机制，确保批量创作时上下文连贯，消除AI写作痕迹。

**Architecture:** 三层状态文件结构（长期状态、短期状态、风格指导）+ 强制读取与验证机制。每次创作前自动读取状态文件，创作后自动更新状态文件，输出附带验证声明。

**Tech Stack:** Markdown文件，skill配置，无需代码。

---

## 文件结构

```
.claude/skills/liexing-xiandao-writing/
├── SKILL.md (更新：新增状态注入流程)
├── references/
│   ├── writing-snapshot.md (新建：短期状态)
│   ├── writing-style-guide.md (新建：风格去AI指导)
│   └── chapter-summary.md (复用现有：长期状态)
```

---

### Task 1: 创建写作状态快照文件

**Files:**
- Create: `.claude/skills/liexing-xiandao-writing/references/writing-snapshot.md`

- [ ] **Step 1: 创建 writing-snapshot.md 文件**

基于第76章结束状态，创建初始状态快照文件：

```markdown
# 写作状态快照

> 本文件记录最新章节的结束状态，确保下一章创作时无缝衔接。
> 每章写完后必须更新此文件。

---

## 最新状态

**最后完成章节**：第76章

### 场景状态
- **地点**：灵域内部灵气聚集点
- **时间**：灾难后第X天，上午
- **天气/环境**：灵气浓郁，四周有异兽活动迹象
- **在场人物**：林衍、楚瑶、赵虎

### 主角状态
- **境界**：地境初期
- **情绪**：平静+期待
- **体力**：充沛
- **灵气储备**：满
- **心理焦点**：测试地境能力，准备探索幸存者聚集点

### 悬念状态
- **上一章结尾悬念**：远处发现人类活动迹象（幸存者聚集点）
- **待处理事件**：探索幸存者聚集点、可能遭遇反派王霸
- **近期目标**：前往幸存者聚集点，了解情况

### 时间线锚点
- **距离上一章**：紧接上一章
- **重要时间节点**：第四部分（第76-100章）为首次逆袭阶段

---

## 状态更新记录

| 章节 | 场景 | 主角情绪 | 悬念 | 更新时间 |
|------|------|----------|------|----------|
| 第76章 | 灵域内部灵气聚集点 | 平静+期待 | 远处人类活动迹象 | 2026-04-21 |
```

- [ ] **Step 2: 验证文件创建成功**

使用Read工具读取创建的文件，确认内容正确。

- [ ] **Step 3: 提交更改**

```bash
git add .claude/skills/liexing-xiandao-writing/references/writing-snapshot.md
git commit -m "feat: add writing-snapshot.md for state injection"
```

---

### Task 2: 创建风格去AI指导文件

**Files:**
- Create: `.claude/skills/liexing-xiandao-writing/references/writing-style-guide.md`

- [ ] **Step 1: 创建 writing-style-guide.md 文件**

创建完整的风格去AI指导文件，包含四个核心模块：对话口语化、描写节奏变化、心理真实感、转折自然化。

```markdown
# 写作风格去AI指导

> 本文件用于消除AI写作的机械感，使小说更贴近自然人类写作风格。

---

## 一、对话口语化

### 问题
AI写的对话太正式、太完整，像写论文而不是人在说话。

### 解决原则

1. **加入语气词**：哼、呵、啧、唔、呃...
2. **使用不完整句**：人说话经常中途打断、省略
3. **加入停顿和犹豫**：用省略号表示思考
4. **方言/习惯用语**：符合人物性格的表达

### 示例对比

**AI写法（问题）**：
```
林衍说："这个遗迹可能存在危险，我们需要谨慎行事。"
楚瑶回答："我同意你的看法，我们可以先用阵法探查内部情况。"
```

**自然写法（改进）**：
```
林衍皱眉："这遗迹......有点不对劲。"
楚瑶轻点头："嗯。我先用阵法探一下？"
```

### 具体技巧

| 技巧 | 示例 |
|------|------|
| 语气词开头 | "呵，你倒是自信。" |
| 短句断句 | "等等——那边有人。" |
| 省略回答 | "嗯。"、"行。" |
| 犹豫停顿 | "这个......让我想想。" |
| 情绪渗入 | "（冷笑）呵。" |

---

## 二、描写节奏变化

### 问题
AI描写每个场景的密度差不多，没有重点和节奏变化。

### 解决原则

1. **重点场景详写**：战斗高潮、情感转折、重大发现
2. **过渡场景简写**：移动、日常、铺垫
3. **用"密度标记"控制**：在章节框架中标记描写密度

### 示例对比

**AI写法（问题）**：所有场景描写密度相同
```
林衍走进废墟。废墟中有很多碎石和残骸。他仔细观察四周。
他发现一个灵气池。灵气池散发着微弱的光芒。
```

**自然写法（改进）**：重点详写，过渡简写
```
林衍踏入废墟。（简写过渡）

他的目光骤然凝固——

前方，一道幽蓝的光芒从地面升腾。那是一个灵气池。

池面泛着涟漪，仿佛某种沉睡的力量正在苏醒。林衍屏住呼吸，他能感到灵气在召唤他......

（详写发现场景）
```

### 描写密度规则

| 场景类型 | 描写密度 | 字数占比 |
|----------|----------|----------|
| 战斗高潮 | 高密度 | 每分钟动作用50-100字 |
| 情感转折 | 高密度 | 情绪变化用100-200字 |
| 重大发现 | 高密度 | 发现过程用100-150字 |
| 场景过渡 | 低密度 | 一句话带过 |
| 日常铺垫 | 低密度 | 2-3句简述 |

---

## 三、心理真实感

### 问题
AI写心理活动太分析化，像"我现在很愤怒，因为..."，不像真实人的思考。

### 解决原则

1. **用感受代替分析**：写"胸口发紧"、"手心出汗"，不写"我感到紧张"
2. **用碎片代替完整**：心理活动是片段的，不是完整论述
3. **用行动暗示心理**：用动作、表情暗示心理状态

### 示例对比

**AI写法（问题）**：
```
林衍感到非常愤怒。他想起了赵虎被王霸欺负的场景，心中充满了复仇的欲望。他决定要让王霸付出代价。
```

**自然写法（改进）**：
```
林衍的拳头攥紧，指节发出轻响。

赵虎......被压在地上的画面，在他脑海中闪过。

"王霸。"他咬牙，一字一顿。

"十倍奉还。"
```

### 心理描写技巧

| 技巧 | 示例 |
|------|------|
| 身体感受 | 胸口发紧、喉咙干涩、手心出汗 |
| 碎片画面 | 画面闪过、声音回响、模糊记忆 |
| 行动暗示 | 攥拳、咬牙、目光锐利 |
| 简短判断 | "不行。"、"有机会。" |

---

## 四、转折自然化

### 问题
AI写的转折太平滑、太"顺利"，缺少真实世界的突兀感。

### 解决原则

1. **情绪突变**：情绪变化往往是突然的，不需要完整铺垫
2. **场景跳跃**：真实世界的时间流逝是跳跃的，不必每个过渡都写
3. **意外打断**：用突发事件打断当前进程

### 示例对比

**AI写法（问题）**：情绪变化太平滑
```
林衍原本很平静，但听到王霸的话后，他逐渐感到愤怒，最终决定反击。
```

**自然写法（改进）**：情绪突变
```
林衍听完，脸色没有变化。

然后——

他的眼神骤然锐利，如刀锋出鞘。

"你说什么？"
```

### 转折技巧

| 类型 | 技巧 | 示例 |
|------|------|------|
| 情绪突变 | 用"骤然"、"突然" | 他的笑容骤然消失 |
| 场景跳跃 | 直接切到新场景，无需过渡 | 三天后。遗迹入口。 |
| 意外打断 | 用突发事件打断 | "等等——有人来了。" |

---

## 五、综合应用示例

### 场景：主角发现敌人埋伏

**AI写法（问题）**：
```
林衍走进山谷。他仔细观察四周的环境，发现有些不对劲。经过分析，他判断可能有敌人埋伏。他感到紧张，对楚瑶说："我们要小心，可能有埋伏。"楚瑶表示同意。
```

**自然写法（改进）**：
```
林衍踏入山谷。

脚步声忽然停住。

他的目光扫过两侧山壁——太安静了。连风声都没有。

"楚瑶。"他低声。

"嗯？"

"......不对劲。"

远处，一道阴影动了。
```

---

## 应用规则

创作时，必须对照本指导检查：

1. 对话是否有口语化元素？
2. 描写密度是否符合场景重要性？
3. 心理活动是用感受而非分析？
4. 转折是否有突兀感？

---

## 输出验证

每章输出时附带风格验证声明：

```
【风格验证】已应用：
- 对话口语化：本次对话共X处使用语气词/短句
- 描写节奏：高密度场景X个，低密度场景X个
- 心理真实感：使用身体感受X处，碎片画面X处
- 转折自然化：情绪突变X处，场景跳跃X处
```
```

- [ ] **Step 2: 验证文件创建成功**

使用Read工具读取创建的文件，确认内容正确。

- [ ] **Step 3: 提交更改**

```bash
git add .claude/skills/liexing-xiandao-writing/references/writing-style-guide.md
git commit -m "feat: add writing-style-guide.md for de-AI style"
```

---

### Task 3: 更新SKILL.md集成状态注入流程

**Files:**
- Modify: `.claude/skills/liexing-xiandao-writing/SKILL.md`

- [ ] **Step 1: 读取现有SKILL.md**

使用Read工具读取现有SKILL.md完整内容，了解当前结构。

- [ ] **Step 2: 在SKILL.md开头添加状态注入流程说明**

在现有"## 写作流程"章节之前，新增"## 写作流程（状态注入版）"章节，替换原有流程：

找到现有"## 写作流程"章节位置，在其之前插入以下内容：

```markdown
## 写作流程（状态注入版）

当用户请求创作时，按以下流程执行：

### 第一阶段：状态注入（必须执行）

1. **读取状态文件**
   - 读取 `references/writing-snapshot.md`（获取当前场景、情绪、悬念）
   - 读取 `references/writing-style-guide.md`（获取风格去AI指导）
   - 读取 `references/chapter-summary.md`（获取长期上下文）

2. **状态确认**
   - 在内部确认关键状态信息已读取
   - 如状态文件不存在或过旧，提醒用户需要先创建/更新

### 第二阶段：构建框架

3. **查阅其他必要资料**
   - 根据本章内容查阅：world-settings、characters、foreshadowing-tracker
   - 检查是否有伏笔可以铺垫或揭示

4. **构建章节框架**
   - 确定本章爽点类型
   - 确定结尾悬念
   - 标记描写密度（高密度场景vs低密度场景）
   - 确定是否有伏笔需要处理

### 第三阶段：正式创作

5. **创作章节正文**
   - 按章节结构规范展开（2000-2500字）
   - **应用风格去AI指导**（对话口语化、描写节奏、心理真实感、转折自然化）
   - 融合辰东+西红柿风格要素

6. **字数验证**（必须执行）
   - 统计正文字数（不含标题和【本章要点】）
   - 如不达标，扩充/精简（最多2轮）

### 第四阶段：输出与更新

7. **输出章节**
   - 输出正文内容
   - **附带状态验证声明**
   - 附带风格验证声明
   - 附带【本章要点】总结

8. **更新状态文件**（必须执行）
   - 更新 `references/writing-snapshot.md`（新的结束状态）
   - 如有境界突破/团队变化，更新 `references/chapter-summary.md`
   - 如有新伏笔，更新 `references/foreshadowing-tracker.md`

---

## 输出格式（状态注入版）

每章输出格式：

```
第X卷 第X章 章节名

[正文内容]

---

【状态验证】已读取：
- 场景：[上一章结束场景]
- 主角情绪：[上一章结束情绪]
- 悬念：[上一章结尾悬念]
- 风格指导已应用

【风格验证】已应用：
- 对话口语化：[X处]
- 描写节奏：[高密度X个，低密度X个]
- 心理真实感：[X处]
- 转折自然化：[X处]

【本章要点】
- 正文字数：XXX字（已验证达标）
- 爽点：XXX
- 悬念/铺垫：XXX
- 境界变化：XXX（若有）
- 收获：XXX（若有）
- 伏笔：XXX（埋下/揭示）
- 下一章预告：XXX
```

---
```

- [ ] **Step 3: 删除原有"## 写作流程"章节**

找到原有的"## 写作流程"章节（不含"状态注入版"），删除该章节完整内容，保留其他章节不变。

原有章节位置大约在第416-485行，删除从"## 写作流程"开始到"## 输出格式"结束的完整内容。

- [ ] **Step 4: 验证修改成功**

使用Read工具读取修改后的SKILL.md，确认：
- 新的"状态注入版"流程已添加
- 原有流程已删除
- 其他章节内容保持不变

- [ ] **Step 5: 提交更改**

```bash
git add .claude/skills/liexing-xiandao-writing/SKILL.md
git commit -m "feat: integrate state injection flow into SKILL.md"
```

---

### Task 4: 最终验证

- [ ] **Step 1: 读取所有相关文件确认完整性**

使用Read工具并行读取：
- writing-snapshot.md
- writing-style-guide.md
- SKILL.md（检查状态注入流程）

- [ ] **Step 2: 验证文件引用关系正确**

确认SKILL.md中的文件引用路径正确：
- `references/writing-snapshot.md`
- `references/writing-style-guide.md`
- `references/chapter-summary.md`

- [ ] **Step 3: 最终提交**

```bash
git status
git log --oneline -5
```

确认所有更改已提交。

---

## 实现顺序说明

按照以下顺序执行任务：

1. Task 1 → 创建 writing-snapshot.md（基础状态文件）
2. Task 2 → 创建 writing-style-guide.md（风格指导文件）
3. Task 3 → 更新 SKILL.md（集成状态注入流程）
4. Task 4 → 最终验证

每个Task完成后才执行下一个Task。

---

## 预期效果

实现完成后：

- 用户说"写一章"时，AI自动读取三个状态文件
- 输出附带状态验证声明和风格验证声明
- 创作后自动更新状态文件
- 无需用户额外操作